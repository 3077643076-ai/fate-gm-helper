# fate-gm-helper 开发规划

## 项目目标
空想圣杯规则的 GM 辅助系统。目标不是替代 GM，而是把“记录、同步、计算、查表”这些重复工作自动化，让看完规则书的人也能比较舒服地开团。

## 最新产品决策
- 项目是本地/私有工具，不准备挂公网；启动方式以简单可靠为准，`bat` 或轻量启动器都可以。
- 战斗双方都应优先从角色卡选择；如果敌人要手填，不如直接建一张角色卡，保持技能、属性、复盘数据完整。
- QQ 群绑定战役需要持久化；之后战役会复用几个群聊，不能因为 Koishi 重启就丢绑定。
- 战斗表最好支持复盘：不一定每次都用，但应能保存一场战斗的编队、工序、技能、手动裁决、胜率和回写结果。
- AI 助手是可选增强项，优先级低于稳定的本地启动、QQ 绑定、角色卡战斗表和复盘。

## 整体框架
- **战前准备**：创建战役、上传角色卡、配置灵脉、绑定 QQ 群。
- **回合推进**：开放行动提交、同步 QQ/Web 行动、整理行动顺序、更新角色状态。
- **战斗结算**：进入战斗表，选择参战位，自动计算属性、战术克制、胜率链条。
- **战后收尾**：保存回合历史、状态继承、判例归档、后续规则查询。

## 当前代码现状

### 已基本完成
- [x] 前端基础：Vue 3 + Vite + Vue Router。
- [x] 后端基础：`backend-node` 使用 Node.js + Express + SQLite（better-sqlite3），主要接口已按 `/api` 暴露，当前主线以轻量部署为准。
- [x] 旧版后端：`backend` 保留 Spring Boot 3.3.5 + JPA + MySQL 实现作为迁移参考，不再作为当前新功能主线。
- [x] QQ 机器人：Koishi 插件 `fate-actions`，支持群绑定战役、从者行动、御主行动。
- [x] 战役管理：创建、选择、删除战役。
- [x] 角色卡管理：支持 .st 文本解析、从者/御主卡保存、检索、删除、退场/重新登场。
- [x] 灵脉管理：支持按战役创建、编辑、删除灵脉，并支持批量分配。
- [x] 回合管理：当前回合、下一回合、关闭当前回合、历史快照。
- [x] 行动提交：Web/QQ 提交行动，前端通过 SSE 实时接收行动提交。
- [x] 角色状态：按战役和回合保存魔力、令咒、异常状态等信息。
- [x] 战斗控制台：集中显示战役、角色、灵脉、当前回合信息、行动顺序、行动记录。

### 战斗表现状
- [x] 已有独立战斗表页面 `/battle-sheet/:campaignId`。
- [x] 蓝方可从已保存从者卡中选择主力位与辅助位。
- [x] 黄方也应从已保存从者卡中选择主力位与辅助位；手动输入只作为临时 fallback，不应成为主要流程。
- [x] 辅助位属性自动减半，并自动计算双方总属性。
- [x] 已选蓝方角色的技能/宝具可展示并勾选发动。
- [x] 支持强击、破袭、试探、扼守战术选择，并自动判断克制关系。
- [x] 支持蓝黄双方战前胜补/胜怯、属性补正、保底输入。
- [x] 支持魔力消耗和魔力不足惩罚展示。
- [x] 已移植一部分 Excel 战斗表公式：属性优劣、基础胜率、最终胜率链条。
- [x] 战斗表数据已接入 SQLite 轻量后端持久化：编队、技能勾选、战术、战前修正、魔力数据、属性结算配置、胜率结果、结算确认锁定。

### 尚未完成 / 需要注意
- [ ] 战斗表目前主要覆盖“战斗开始时”和基础胜率链条，还没有完整覆盖初始工序、主要工序、最终工序。
- [x] 技能模板已接入结构化模板库：Node/SQLite 后端提供 CRUD，前端可录入模板，战斗表按技能名匹配并自动套用简单数值效果。
- [x] 魔力消耗目前能计算不足惩罚，并在结算确认后把蓝方参战角色剩余魔力写回当前回合角色状态。
- [ ] 状态效果有基础记录能力，但还没有和战斗表技能/结算自动联动。
- [ ] Q&A 判例系统尚未实现。
- [ ] AI 规则助手尚未实现。
- [ ] 轻量后端已切换为 Node + SQLite；Spring Boot 版保留参考，但新功能优先写 `backend-node`。

## 后续开发计划

### 阶段 1：战斗表 V1 收尾（当前最优先）
**目标**：让一场“战斗开始时”的结算可以稳定跑通、保存、刷新恢复。

- [x] 蓝方/黄方参战人员选择：双方都以角色卡选择为主，黄方手动录入只作为临时 fallback。
- [x] 自动填入/录入属性，并计算辅助位减半后的总值。
- [x] 展示已选角色技能，GM 勾选是否发动。
- [x] 战术选择与克制关系判断。
- [x] 战前胜补/胜怯、属性补正、保底输入。
- [x] 基础胜率与最终胜率计算。
- [x] 战斗表主要输入保存到后端。
- [x] 补一个“结算确认/锁定”动作，明确当前战斗表已经定稿。
- [x] 把战斗结果/消耗结果回写到角色状态，形成“战斗表 → 状态更新”的闭环。
- [x] 增加战斗表读取失败、无当前回合、无角色卡时的清晰提示。

### 阶段 2：技能模板结构化存储
**目标**：让技能从“展示文本”进化到“可辅助结算”。

**存储方案仍采用两层：**
- **结构层**：可量化字段。
  - 技能名、等级、类型/面向。
  - 发动时机：常驻、随时、战斗开始时、初始工序、主要工序、最终工序等。
  - 消耗魔力、回转值。
  - 属性补正，例如筋力 +10、耐久 +20。
  - 胜率补正/惩罚，例如己方 +20%、敌方 -10%。
  - 状态赋予，例如中毒 1 层。
  - 战斗位限定，例如主力位、辅助位、支援位。
- **原文层**：完整保留技能原文，方便 GM 手动裁决复杂条件。

**推荐落地顺序：**
- [x] 新建 `skill_template` SQLite 表和 `backend-node` CRUD 路由。
- [x] 前端增加技能模板录入/检索页面，先支持手动录入实际会用到的技能。
- [x] 角色卡技能名匹配模板库，匹配成功后在战斗表中显示结构化效果。
- [x] GM 勾选技能后，自动把简单数值效果加入战斗表；复杂条件仍由 GM 手动判断。

### 阶段 3：完整战斗工序
**目标**：把战斗表从“开战计算器”扩展成“完整战斗结算助手”。

- [ ] 初始工序：技能发动、响应、胜率/属性变化。
- [x] 主要工序：已增加主要工序专用摘要面板，集中展示宝具/核心技能、需裁决项和已生效项。
- [ ] 主要工序：核心技能和宝具的完整响应处理。
- [x] 工序确认体验：编队、常驻、战斗开始时、初始、主要、最终工序支持确认；结算锁定前可撤回确认，撤回某工序会同步撤回后续已确认工序，锁定后不可改。
- [ ] 最终工序：最终补正、保底、决胜检定前汇总。
- [x] 魔力消耗自动汇总，并支持确认后写回角色状态。
- [x] 状态效果基础联动：确认结算时可把明确结构化的状态效果写回角色状态，复杂目标仍保留 GM 手动裁决。
- [x] 战斗记录快照：保存本次战斗双方角色卡、各阶段输入、发动技能、GM 手动裁决、最终结果、状态效果和资源回写，方便复盘。
- [x] 战斗表页面拆分：编队与属性、战斗开始时、初始工序、最终工序、决胜结算、复盘历史、当前工序效果、GM 手动修正已拆成子组件。

### 阶段 3.5：QQ 绑定持久化
**目标**：让 QQ 群和战役的绑定重启后仍然保留，适合长期复用固定群聊开团。

- [x] 后端新增 QQ 群绑定存储，至少保存 platform、guildId、campaignId、群名、更新时间。
- [x] Koishi 插件从后端查询绑定，不再只依赖内存 Map。
- [x] `.绑定战役 <战役ID>` 写入持久化绑定；重复绑定时更新旧记录。
- [x] 增加查询当前绑定的指令，避免 GM 不知道某群当前绑定到哪个战役。
- [x] 保留手动改绑能力，方便复用同一批 QQ 群跑后续战役。

### 阶段 4：Q&A 判例系统
**目标**：把跑团时遇到的规则争议记录下来，后续可查。

- [ ] 后端新增判例实体：问题、答案、相关技能、相关战役、状态、创建时间。
- [ ] 战斗控制页增加侧边栏 Q&A 面板。
- [ ] 支持 GM 手动记录问题和答案。
- [ ] 支持关键词搜索已有判例。
- [ ] QQ 指令 `.q <问题>` 或 `.提问 <问题>`：玩家提交问题。
- [ ] GM 在页面查看新问题、回复、归档。

### 阶段 5：AI 规则助手
**目标**：在 GM 遇到规则疑问时提供参考答案，但不直接替 GM 裁决。

- [ ] 页面内嵌 AI 对话框。
- [ ] 可把规则书、判例、技能原文作为上下文。
- [ ] AI 输出必须标注“建议”，由 GM 审核后执行。
- [ ] AI 不直接修改战斗表、不自动判定胜负、不自动消耗资源。

## 关键技术决策
1. **主线后端以 Node + SQLite 为准**：`backend-node` 是当前轻量部署主线，使用 Express + better-sqlite3，减少服务器内存占用；Spring Boot/MySQL 版只作旧实现参考。
2. **复杂规则不强行全自动化**：数值明确的部分自动算，条件复杂、冲突裁决、规则争议交给 GM。
3. **技能效果采用“结构层 + 原文层”**：能量化的字段参与计算，不能稳定量化的效果只展示给 GM。
4. **战斗表替代旧 Excel 的路线是逐步移植**：先移植常用公式和流程，不一次性追求完整复刻。
5. **QQ 侧只做输入入口，但绑定必须持久化**：`.绑定战役`、`.从者行动`、`.御主行动` 已有；绑定战役要能跨重启保留，Q&A 指令后续再补。
6. **AI 只做可选规则助手**：给建议、查资料、解释规则，不直接进入自动结算链路，优先级低于核心跑团流程。

## 技术栈
- 前端：Vue 3 + Vite + Vue Router。
- 主后端：Node.js + Express + SQLite（better-sqlite3）。
- 旧版后端：Spring Boot 3.3.5 + JPA + MySQL（保留参考）。
- 机器人：Koishi + OneBot / NapCat 接 QQ。
- 数据库：SQLite，默认文件为 `backend-node/data/gm_helper.db`，可通过 `FATE_GM_DB_PATH` 放到 Koishi 项目相邻目录或统一数据目录。

## 近期推荐任务
1. 用 `backend-node` 启动轻量 Express + SQLite 后端，确保服务器不用 MySQL/JDK。
2. 修正旧启动脚本和说明，确保本地启动只走 `backend-node` + 构建后的前端。
3. 确认战斗表双方都以角色卡选择为主，弱化手动敌人输入。
4. 先录入 5-10 个实际会用到的技能模板，验证技能匹配和数值自动套用。
5. 扩展初始/主要/最终工序的响应处理，不要一开始就做 AI。
6. 补齐复杂状态效果目标选择、持续时间、层数增减和 GM 手动覆盖。
7. 再做 Q&A 判例系统。
